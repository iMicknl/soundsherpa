// SoundSherpaCore
//
// Pure, hardware-free building blocks for SoundSherpa: data models, device-protocol
// codecs, and device identification. This module imports only Foundation — never
// IOBluetooth or AppKit — so every type here is unit-testable without a Mac, Bluetooth,
// or a connected headset attached.
